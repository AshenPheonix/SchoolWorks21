#pragma once

#include <algorithm>
#include "CinReader.h"
#include <cmath>
#include <sstream>

class Activator{
public:
    unsigned int getPrime(unsigned int);
    unsigned int sumTheDigits(unsigned int);
    
};